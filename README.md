"# celine.thiombiano.m1ecap" 
Le premier commit de ce répertoire a été effectué le 16/04/2026 à 9h36_


"## Historique

#| eval: false
git checkout master
git commit -m "commit A"

git checkout -b feature
git commit -m "commit B"

git checkout master
git commit -m "commit C"

git merge feature -m "merge commit"

git commit -m "commit D"
